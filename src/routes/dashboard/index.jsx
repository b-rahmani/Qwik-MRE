import { component$ } from '@builder.io/qwik'

const Index = component$(({ }) => {

    return <div class={'flex flex-col sm:flex-row max-w-6xl mx-auto bg-gray-100 rounded-md my-10'}>
        <span>Dashboard</span>
    </div>
})

export default Index
